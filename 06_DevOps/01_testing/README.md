# Infos

Generate JWT secret by 
* `openssl rand -base64 64`, 
* or `openssl rand -base64 32`,
* or `openssl rand -hex 64`.
More: https://dev.to/talaamm/generate-a-random-string-for-a-jwt-secret-2g1e

